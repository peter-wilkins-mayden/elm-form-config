db-uri = "postgres://postgrest:temporary_password@localhost/aggy"
db-schema = "api"
db-anon-role = "web_anon"


db-uri = "postgres://localhost:5432/postgrest_test"
db-pool = 10

server-root = "./client-example"
server-host = "*4"
server-port = 3000

## choose a secret to enable JWT auth
## (use "@filename" to load from separate file)
jwt-secret = "foo"
secret-is-base64 = false
